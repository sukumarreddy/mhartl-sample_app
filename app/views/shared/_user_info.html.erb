<%# Listing 10.32 %>
<a href="<%= user_path(current_user) %>">
  <%= gravatar_for current_user, size: 52 %> <%# remember gravatar_for()? Listings 7.29, 9.25 %>
</a>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <%= link_to "view my profile", current_user %>
</span>
<span>
  <%= pluralize(current_user.microposts.count, "micropost") %> <%# Microposts (1) was forgiveable as a label, but this... %>
</span>