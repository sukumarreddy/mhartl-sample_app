<%# -- removed in Listing 4.5 for who knows what reason <% provide(:title, 'Home') %>

<%# Listing 10.31 %>
<% if signed_in? %>
  <div class="row">
    <aside class="span4">
      <section>
        <%= render 'shared/user_info' %>
      </section>
      <section>
        <%= render 'shared/micropost_form' %>
      </section>
    </aside>
  </div>  
<% else %>
  <%# Listing 5.2 %>
  <div class="center hero-unit"> <%# classes used by Bootstrap %>
    <h1>Sample App</h1>
    <h2>
      <p>Find me in app/views/static_pages/home.html.erb</p>
      <p>
        Listing 3.10: <br/>
        This is the home page for the
        <a href="http://railstutorial.org/">Ruby on Rails Tutorial</a>
        sample application.
      </p>
    </h2>
    <%= link_to "Sign up now!", signup_path, class: "btn btn-large btn-primary" %> <%# stubbed for chapter 7 %>
  </div>
  <%= link_to image_tag("rails.png", alt: "Rails"), 'http://rubyonrails.org/' %> <%# Rails' image_tag helper %>
<% end %>