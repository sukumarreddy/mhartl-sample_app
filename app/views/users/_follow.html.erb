<%# Listing 11.25 %>
<%# Listing 11.35 adds "remote: true" %>
<%= form_for(current_user.relationships.build(followed_id: @user.id), remote: true) do |f| %> <%# build() sends POST %>
  <div><%= f.hidden_field :followed_id %></div>
  <%= f.submit "Follow", class: "btn btn-large btn-primary" %> 
<% end %>